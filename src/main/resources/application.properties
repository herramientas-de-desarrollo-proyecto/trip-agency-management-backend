spring.application.name=tripagencymanagement
spring.profiles.active=dev
server.servlet.context-path=/ptc/api/

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

#Datbase configuration CONNECTION
spring.datasource.driver-class-name=org.postgresql.Driver

#SQL queries logging config
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#documentación
# swagger-ui custom path
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs

#Pagination
spring.data.web.pageable.one-indexed-parameters=true

# ========================================
# JWT Configuration
# ========================================
# Secret key for JWT signing (Base64 encoded, min 256 bits for HS256)
# IMPORTANT: Change this in production
jwt.secret-key=dGhpc2lzYXZlcnlsb25nc2VjcmV0a2V5Zm9yand0c2lnbmluZ3B1cnBvc2VzYW5kaXRtdXN0YmVhdGxlYXN0MjU2Yml0cw==

# Access token expiration in milliseconds (15 minutes = 900000)
jwt.access-token-expiration=900000

# Refresh token expiration in milliseconds (7 days = 604800000)
jwt.refresh-token-expiration=604800000

# JWT Issuer
jwt.issuer=ptc-agency